public class UniquePath {
    // m rows, n columns
    public static int uniquePaths(int m, int n) {
        /*
        if (m!=0 && n!=0) {
            System.out.printf("%d,%d\n", m, n);
        }
         */
        return -1;
    }
}
